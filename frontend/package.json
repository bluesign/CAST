{
  "name": "cast-frontend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "prettier": "export SET NODE_OPTIONS=--openssl-legacy-provider && yarn workspace @cast/client prettier",
    "build": "export SET NODE_OPTIONS=--openssl-legacy-provider && yarn workspace @cast/client build --max-old-space-size=12288",
    "start": "export SET NODE_OPTIONS=--openssl-legacy-provider && yarn workspace @cast/client start"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "dependencies": {},
  "resolutions": {
    "babel-loader": "8.1.0",
    "react-error-overlay": "6.0.9"
  }
}
